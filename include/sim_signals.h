/**
 * @file  sim_signals.h
 * @brief Симуляция сигналов трек-заезда.
 *
 * Порт web/src/mock/signalGenerator.ts один-в-один: те же формулы, те же
 * константы, те же id. Благодаря этому картинка на устройстве совпадает с
 * предпросмотром в редакторе, и любое расхождение сразу указывает на баг
 * рендера, а не на разные данные.
 *
 * Включается флагом "sim": { "enabled": true } в JSON лейаута.
 */

#pragma once

#include <Arduino.h>

namespace Sim {

/// Пересчитать все сигналы и записать их в шину.
/// @param t секунды с момента старта (дробные)
void update(float t);

} // namespace Sim
